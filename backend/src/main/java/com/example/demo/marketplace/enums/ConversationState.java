package com.example.demo.marketplace.enums;

public enum ConversationState {
    IDLE,
    AWAITING_BUYER_SELECTION,
    AWAITING_SELLER_RESPONSE,
    AWAITING_SELLER_QUICK_REPLY,
    AWAITING_BUYER_PAYMENT,
    COMPLETED,
    CLOSED
}
